# Architecture & Product Decisions

| Date | Decision | Status |
|------|----------|--------|
| 2026-08-08 | Use Google environment as the initial platform direction | Accepted |
| 2026-08-08 | Support multiple WhatsApp numbers / WABAs | Accepted |
| 2026-08-08 | Existing Meta Business Portfolio and WABAs will be used | Accepted |
| 2026-08-08 | Keep conversations unread until the assigned user responds | Accepted |
| 2026-08-08 | Phase 1 authenticates through the active Google Workspace identity; no password or provider authentication is stored by the app | Accepted |
| 2026-08-08 | Use centralized permission checks and repository contracts; use Script Properties only as the initial administrative persistence adapter | Accepted |
| 2026-08-08 | Treat number IDs as opaque references until a later provider-integration phase | Accepted |
| 2026-08-08 | Phase 1 has exactly five fixed roles: ADMIN, SUPERVISOR, SITE_MANAGER, AGENT, and VIEWER; their permission matrix is code and documentation, not runtime-configurable | Accepted |
| 2026-08-09 | Consolidated the master 21-phase roadmap (`docs/ROADMAP.md`); Phase 2 is redefined as the Core Database/Repository Layer, inserted before WhatsApp/Exotel integration (now Phase 3). Supersedes the earlier coarser phase numbering | Accepted |
| 2026-08-09 | `appsscript.json` declares `executionApi.access = MYSELF` and explicit `oauthScopes` (`script.storage`, `userinfo.email`) so the Apps Script API can run administrative one-off functions under the developer's own identity | Accepted |
| 2026-08-09 | Phase 1 bootstrap must be executed via the Apps Script editor's Run button (temporary wrapper function, deleted after use), not `clasp run` — the script is bound to Apps Script's auto-managed default GCP project, and `scripts.run` (Execution API) rejects calls against that project regardless of granted OAuth scopes. A standard user-managed GCP project would be required to use `clasp run` instead | Accepted |
| 2026-08-09 | Phase 2 does not migrate Phase 1's Users/Teams/Team_Members/User_Number_Access/Audit_Log off `PropertiesRepository`; only the new CRM-scale entities get the Sheets-backed repository layer. Any future migration is its own separately-approved change | Accepted |
| 2026-08-09 | Phase 2 ships repository code and schema contracts only, verified with Node-mocked tests; no real Google Spreadsheet is provisioned and `wap.phase2.spreadsheetId` stays unset until Phase 3, the first phase that needs to persist real data | Accepted |
| 2026-08-09 | Resolved the roadmap's ambiguous Phase 2 repository-class list against its entity list: `AccessRepository` covers `Number_Assignment_Config` + `Number_Assignment_Users` (not `User_Number_Access`, which Phase 1 already covers); `AssignmentRepository` covers `Conversation_Assignments`; added `QuickReplyRepository` for `Quick_Replies`, which the roadmap's entity list named but its repository-class list omitted. See `docs/DATABASE.md` for the full mapping | Accepted |
| 2026-08-09 | Phase 2's backing spreadsheet is now provisioned ahead of Phase 3: user created spreadsheet `1qugfpq7dfNd2phwb8GVh_6VEsDe1Kf0fd76w3JQcqt4` and configured it as Script Property `SPREADSHEET_ID` directly in the Apps Script project. `Phase2Spreadsheet.SCRIPT_PROPERTY` in `src/Phase2Persistence.gs` was updated to read `SPREADSHEET_ID` (not the originally planned `wap.phase2.spreadsheetId`) to match what's actually configured, rather than asking for a rename | Accepted |
| 2026-08-09 | `appsscript.json` also needs `https://www.googleapis.com/auth/spreadsheets` in `oauthScopes` (discovered via live smoke test — `SpreadsheetApp.openById` otherwise fails with a permissions error even with the sheet's ID correctly configured) | Accepted |
| 2026-08-09 | `SheetRepository` writes must call `Range.setNumberFormat('@')` directly on the exact target range immediately before `setValues()`, on every write — not just once at sheet creation, and not via `sheet.appendRow()`. Google Sheets otherwise silently coerces numeric-looking string values (e.g. a phone number `"000"`) into real numbers, discovered live because the Node-mocked test can't simulate Sheets' own type coercion | Accepted |
| 2026-08-09 | Phase 2 is fully verified live against the real spreadsheet (not just Node-mocked): temporary `src/Phase2SmokeTestTemp.gs` ran a full create/read/count/remove cycle via `NumberRepository` against `1qugfpq7dfNd2phwb8GVh_6VEsDe1Kf0fd76w3JQcqt4`, confirmed correct, then deleted | Accepted |
| 2026-08-09 | Phase 3 registers the 10 known WhatsApp numbers now with only what's already known (`displayName`, `phoneNumber`, `provider: 'exotel'`); `providerAccountId`/`wabaId`/`providerNumberId` are seeded as empty strings and filled in later via `updateNumber` once the user supplies the real Exotel values — doesn't block building the provider abstraction | Accepted |
| 2026-08-09 | Exotel credentials are read from Script Properties `EXOTEL_API_KEY`, `EXOTEL_API_TOKEN`, `EXOTEL_ACCOUNT_SID`, `EXOTEL_SUBDOMAIN` (flat naming, matching `SPREADSHEET_ID`'s precedent, not the `wap.*` namespace), set directly by the user in the Apps Script editor — never pasted into chat | Accepted |
| 2026-08-09 | `ExotelProvider`'s exact request/response field names are **unverified**. Confirmed from public docs: base URL `https://<subdomain>/v2/accounts/<account_sid>/messages` and HTTP Basic Auth with API key/token; status codes 30001/30002/30003 = Sent/Delivered/Seen. Everything else (message envelope shape, template/media field names, webhook payload shape) is a best-effort guess modeled on WhatsApp Cloud API conventions, flagged inline in `src/Phase3ExotelProvider.gs` as UNVERIFIED — Exotel's detailed API reference pages 404'd on fetch (likely JS-rendered). Must be corrected via live testing once `EXOTEL_*` Script Properties are set, the same way Phase 2's live smoke test caught two real bugs Node mocks couldn't | Accepted |
| 2026-08-09 | Phase 3 introduces `Phase3Api` (`src/Phase3Services.gs`) as the first real, authorized caller of a Phase 2 repository (`NumberRepository`) — `createNumber`/`updateNumber`/`listNumbers` all require `Phase1Permissions.NUMBERS_ADMIN` via the existing Phase 1 `AccessControl`, reusing the same authorization/audit stack rather than inventing Phase-3-specific permission logic | Accepted |
| 2026-08-09 | User asked for a sheet tab to hold Exotel credentials; declined per `docs/SECURITY.md`'s existing rule ("Never: ... Google Sheet cells ... Store credentials through appropriate secure configuration") and the fact that later phases (Phase 12 Admin Panel) give Site Managers/Supervisors access to this spreadsheet. Credentials stay in Script Properties only. Compromise: `refreshExotelConfigStatus()` (`src/Phase3ExotelConfigStatus.gs`) writes a non-secret `Exotel_Config_Status` tab listing only property NAMES and whether each is set (YES/NO) — never the values | Accepted |
| 2026-08-09 | `ExotelProvider.getTemplates()` is now **live-confirmed** against the user's real account (WABA `1359198589697291`, two real approved templates returned). Confirmed: endpoint is `GET /v2/accounts/<sid>/templates?waba_id=<waba_id>` (not `/whatsapp/templates` — an earlier assumption that 404'd), `waba_id` is a mandatory query parameter (Exotel returns a clear `"WabaID is mandatory"` error otherwise, not silently ignored), and the full response envelope shape (see `src/Phase3ExotelProvider.gs` inline comment). `sendText`/`sendMedia`/`sendTemplate`/`createTemplate`/`getMessageStatus` remain unverified — verifying those means actually sending a real message or creating a real template (real cost/effect), deliberately deferred to Phase 6 rather than tested speculatively now | Accepted |
| 2026-08-09 | User populated 8 of 10 numbers' `providerAccountId`/`wabaId`/`providerNumberId` (all share `providerAccountId: 'echt61'`) by editing the `WhatsApp_Numbers` sheet directly rather than through `updateNumber()`. This bypassed the audit log (no `number.updated` entries for these edits) — acceptable now since the user is the sole admin, but a reminder that once more users have write access, data changes should go through the authorized endpoint so the audit trail stays complete. `Spreewalk - Raipur` and `ECHT Advisory` still have blank provider fields, to be filled in later | Accepted |
| 2026-08-09 | Phase 4's webhook (`doPost` in `src/Phase4Webhook.gs`) is authenticated with a shared secret token in the URL (`?token=...`, Script Property `WEBHOOK_SECRET_TOKEN`), not Phase 1's `AccessControl` — Exotel is not a Google Workspace identity, so `Session.getActiveUser()`-based checks don't apply to webhook calls. Chosen over researching Exotel's own signature-verification scheme (unconfirmed whether it exists) for simplicity and to avoid another research/verification cycle | Accepted |
| 2026-08-09 | The webhook gets its own dedicated Web App deployment (`Execute as: Me`, `Access: Anyone`) rather than reusing/reconfiguring `Test_V01` — keeps `Test_V01` (`Anyone within ECHT`) untouched and reserved for a future admin-panel UI, mirroring how the Execution API got its own deployment in Phase 1 | Accepted |
| 2026-08-09 | `Phase4Api.ingestInboundMessage` also handles status-callback-shaped payloads (delivery receipts for messages sent in Phase 6), branching on `normalized.direction !== 'INBOUND'`, since Exotel delivers both event types to the same webhook URL. Applies the status update to the matching message by `providerMessageId` if found, otherwise a harmless no-op — this is the only Phase-6-adjacent logic Phase 4 does, kept minimal rather than building any outbound-sending awareness | Accepted |
| 2026-08-09 | The webhook deployment required two live fixes: (1) `appsscript.json` needed an explicit `webapp` block — without one, `clasp deploy` only offered an "API executable" entry point (no HTTP-reachable URL at all), confirmed by checking Manage Deployments in the Apps Script UI; (2) `webapp.access: "ANYONE"` maps to "Anyone **with a Google Account**" (still requires Google sign-in) — the correct value for a truly public, unauthenticated endpoint Exotel can POST to is `"ANYONE_ANONYMOUS"`, which the UI displays simply as "Anyone". Both caught by visually checking Manage Deployments before pointing Exotel at the URL, not assumed | Accepted |
| 2026-08-09 | Discovered while managing deployments: `Test_V01` is now **archived** and `Test_V02` is the **active** one (previously flagged as an unexplained deployment) — origin of `Test_V02` still unconfirmed by the user, but it's now clear it's the currently-active deployment, not a stray unused one | Accepted |
| 2026-08-10 | Added `Webhook_Debug_Log` sheet tab (`logWebhookDebug_` in `src/Phase4Webhook.gs`) logging every `doPost` call's params/body/outcome — kept as a permanent lightweight diagnostic (not a temp file), since checking a spreadsheet proved far easier than the Apps Script Executions panel for calls that didn't originate from the editor's Run button | Accepted |
| 2026-08-10 | `ExotelProvider.processWebhook()` is now **live-confirmed** against a real inbound WhatsApp message via Exotel's WhatsApp Console webhook. Two corrections from the original best-effort guess: (1) the message id field is `sid`, not `id`/`message_sid`; (2) `Phase4Api`'s number lookup needed to change entirely — Exotel's webhook `to` field is the actual E.164 phone number (e.g. `+917948502801`), not the `providerNumberId` (Meta "Phone Profile" ID) captured during Phase 3 registration. Numbers are now matched by comparing the last 10 digits of `phoneNumber` against the incoming value (`normalizePhoneTail_` in `src/Phase4Services.gs`), absorbing formatting differences between our stored `"079-485-02801"` style and Exotel's `"+917948502801"`. Also confirmed: `profile_name` carries the sender's real WhatsApp display name, now used to seed a new customer's `name` field | Accepted |
| 2026-08-10 | **Phase 4 fully live-verified end-to-end**: a real WhatsApp message ("Hola" from a real number) correctly created a `Customer` (name "Eva" from `profile_name`), an `OPEN` `Conversation` linked to the right registered number, and an `INBOUND` `Message` — all visible in the live spreadsheet. Status-callback handling (`applyStatusUpdate_`) remains unverified live, since triggering one requires actually sending a message, which is Phase 6's job | Accepted |
| 2026-08-10 | Deleted the unexplained `Test_V02` deployment (`AKfycbxbvO6...`) — user confirmed it's safe to remove ("just a demo from Exotel"). The pre-existing ngrok callback URL on all 10 numbers in Exotel is also confirmed safe to remove, left as a manual cleanup item since it's per-number config in Exotel's dashboard, not something scriptable from this repo | Accepted |
| 2026-08-10 | Phase 5's `Conversations` entity has no `teamId` field, but `AccessControl.requireConversationOperation` needs one for SUPERVISOR/SITE_MANAGER team-scoped checks. Resolved by deriving team scope from number access on the fly (`Phase5Api.resolveTeamIdForNumber_`) — a Supervisor/Site Manager can view a conversation if its `numberId` is covered by a team they're an active member of / own (`Team_Members.numberIds`), computed per request rather than adding `teamId` to the schema | Accepted |
| 2026-08-10 | Confirmed via close reading of `Phase1AccessControl.gs`: every non-admin role, including SUPERVISOR/SITE_MANAGER, needs an explicit `numberAccess` grant before anything else is checked (`hasGrantedNumber_`). Team `numberIds` is a separate, additional scope only for the team-view path. This makes `listMyNumbers()` uniform across all non-admin roles — no team-derivation needed just to decide which numbers appear in the picker | Accepted |
| 2026-08-10 | `Phase5Api.listConversations` gates on basic number access **before** filtering conversations, matching `AccessControl`'s own tamper-protection pattern (explicit `FORBIDDEN` for a completely inaccessible `numberId`, not a silently empty list) — caught by a test that expected a throw and got an empty array instead, revealing the per-conversation `try/catch` alone wasn't enough | Accepted |
| 2026-08-10 | `.claspignore` excluded `frontend/**` entirely (set up in Phase 0 when the folder was just a placeholder) — removed now that Phase 5 actually needs to push `frontend/Index.html`. Worth remembering this could have silently blocked frontend deploys with no error if not caught before the first push | Accepted |
| 2026-08-10 | Phase 5 UI deliberately omits stage/remarks/reminder panels even though the roadmap's mockup shows them — those entities exist in storage (Phase 2) but have no service layer until Phase 8/9. Showing non-functional placeholder UI was ruled out as a half-finished implementation | Accepted |
| 2026-08-10 | `phase5-admin-ui` deployment: `Execute as: Me`, `Access: Anyone within ECHT`. Initially assumed `Execute as: User accessing the web app` would be needed, but confirmed `Execute as: Me` is architecturally correct here — with `User accessing the web app`, every agent would need direct Google Sheets edit access (since `SpreadsheetApp` calls would run under their own Google authority), letting them bypass `AccessControl` entirely by opening the Sheet directly. `Execute as: Me` keeps direct Sheet access restricted to the developer only; `Session.getActiveUser()` still correctly reports each visitor's real identity because access is domain-restricted (not fully anonymous), so per-user role checks still work correctly — **live-verified 2026-08-10**: three-pane UI loaded all 10 numbers, the real conversation, and the real "Hola" message from Phase 4's test | Accepted |
