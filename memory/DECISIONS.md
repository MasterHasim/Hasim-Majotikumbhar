# Architecture & Product Decisions

| Date | Decision | Status |
|------|----------|--------|
| 2026-08-08 | Use Google environment as the initial platform direction | Accepted |
| 2026-08-08 | Support multiple WhatsApp numbers / WABAs | Accepted |
| 2026-08-08 | Existing Meta Business Portfolio and WABAs will be used | Accepted |
| 2026-08-08 | Keep conversations unread until the assigned user responds | Accepted |
| 2026-08-08 | Phase 1 authenticates through the active Google Workspace identity; no password or provider authentication is stored by the app | Accepted |
| 2026-08-08 | Use centralized permission checks and repository contracts; use Script Properties only as the initial administrative persistence adapter | Accepted |
| 2026-08-08 | Treat number IDs as opaque references until a later provider-integration phase | Accepted |
| 2026-08-08 | Phase 1 has exactly five fixed roles: ADMIN, SUPERVISOR, SITE_MANAGER, AGENT, and VIEWER; their permission matrix is code and documentation, not runtime-configurable | Accepted |
| 2026-08-09 | Consolidated the master 21-phase roadmap (`docs/ROADMAP.md`); Phase 2 is redefined as the Core Database/Repository Layer, inserted before WhatsApp/Exotel integration (now Phase 3). Supersedes the earlier coarser phase numbering | Accepted |
| 2026-08-09 | `appsscript.json` declares `executionApi.access = MYSELF` and explicit `oauthScopes` (`script.storage`, `userinfo.email`) so the Apps Script API can run administrative one-off functions under the developer's own identity | Accepted |
| 2026-08-09 | Phase 1 bootstrap must be executed via the Apps Script editor's Run button (temporary wrapper function, deleted after use), not `clasp run` — the script is bound to Apps Script's auto-managed default GCP project, and `scripts.run` (Execution API) rejects calls against that project regardless of granted OAuth scopes. A standard user-managed GCP project would be required to use `clasp run` instead | Accepted |
| 2026-08-09 | Phase 2 does not migrate Phase 1's Users/Teams/Team_Members/User_Number_Access/Audit_Log off `PropertiesRepository`; only the new CRM-scale entities get the Sheets-backed repository layer. Any future migration is its own separately-approved change | Accepted |
| 2026-08-09 | Phase 2 ships repository code and schema contracts only, verified with Node-mocked tests; no real Google Spreadsheet is provisioned and `wap.phase2.spreadsheetId` stays unset until Phase 3, the first phase that needs to persist real data | Accepted |
| 2026-08-09 | Resolved the roadmap's ambiguous Phase 2 repository-class list against its entity list: `AccessRepository` covers `Number_Assignment_Config` + `Number_Assignment_Users` (not `User_Number_Access`, which Phase 1 already covers); `AssignmentRepository` covers `Conversation_Assignments`; added `QuickReplyRepository` for `Quick_Replies`, which the roadmap's entity list named but its repository-class list omitted. See `docs/DATABASE.md` for the full mapping | Accepted |
| 2026-08-09 | Phase 2's backing spreadsheet is now provisioned ahead of Phase 3: user created spreadsheet `1qugfpq7dfNd2phwb8GVh_6VEsDe1Kf0fd76w3JQcqt4` and configured it as Script Property `SPREADSHEET_ID` directly in the Apps Script project. `Phase2Spreadsheet.SCRIPT_PROPERTY` in `src/Phase2Persistence.gs` was updated to read `SPREADSHEET_ID` (not the originally planned `wap.phase2.spreadsheetId`) to match what's actually configured, rather than asking for a rename | Accepted |
| 2026-08-09 | `appsscript.json` also needs `https://www.googleapis.com/auth/spreadsheets` in `oauthScopes` (discovered via live smoke test — `SpreadsheetApp.openById` otherwise fails with a permissions error even with the sheet's ID correctly configured) | Accepted |
| 2026-08-09 | `SheetRepository` writes must call `Range.setNumberFormat('@')` directly on the exact target range immediately before `setValues()`, on every write — not just once at sheet creation, and not via `sheet.appendRow()`. Google Sheets otherwise silently coerces numeric-looking string values (e.g. a phone number `"000"`) into real numbers, discovered live because the Node-mocked test can't simulate Sheets' own type coercion | Accepted |
| 2026-08-09 | Phase 2 is fully verified live against the real spreadsheet (not just Node-mocked): temporary `src/Phase2SmokeTestTemp.gs` ran a full create/read/count/remove cycle via `NumberRepository` against `1qugfpq7dfNd2phwb8GVh_6VEsDe1Kf0fd76w3JQcqt4`, confirmed correct, then deleted | Accepted |
| 2026-08-09 | Phase 3 registers the 10 known WhatsApp numbers now with only what's already known (`displayName`, `phoneNumber`, `provider: 'exotel'`); `providerAccountId`/`wabaId`/`providerNumberId` are seeded as empty strings and filled in later via `updateNumber` once the user supplies the real Exotel values — doesn't block building the provider abstraction | Accepted |
| 2026-08-09 | Exotel credentials are read from Script Properties `EXOTEL_API_KEY`, `EXOTEL_API_TOKEN`, `EXOTEL_ACCOUNT_SID`, `EXOTEL_SUBDOMAIN` (flat naming, matching `SPREADSHEET_ID`'s precedent, not the `wap.*` namespace), set directly by the user in the Apps Script editor — never pasted into chat | Accepted |
| 2026-08-09 | `ExotelProvider`'s exact request/response field names are **unverified**. Confirmed from public docs: base URL `https://<subdomain>/v2/accounts/<account_sid>/messages` and HTTP Basic Auth with API key/token; status codes 30001/30002/30003 = Sent/Delivered/Seen. Everything else (message envelope shape, template/media field names, webhook payload shape) is a best-effort guess modeled on WhatsApp Cloud API conventions, flagged inline in `src/Phase3ExotelProvider.gs` as UNVERIFIED — Exotel's detailed API reference pages 404'd on fetch (likely JS-rendered). Must be corrected via live testing once `EXOTEL_*` Script Properties are set, the same way Phase 2's live smoke test caught two real bugs Node mocks couldn't | Accepted |
| 2026-08-09 | Phase 3 introduces `Phase3Api` (`src/Phase3Services.gs`) as the first real, authorized caller of a Phase 2 repository (`NumberRepository`) — `createNumber`/`updateNumber`/`listNumbers` all require `Phase1Permissions.NUMBERS_ADMIN` via the existing Phase 1 `AccessControl`, reusing the same authorization/audit stack rather than inventing Phase-3-specific permission logic | Accepted |
| 2026-08-09 | User asked for a sheet tab to hold Exotel credentials; declined per `docs/SECURITY.md`'s existing rule ("Never: ... Google Sheet cells ... Store credentials through appropriate secure configuration") and the fact that later phases (Phase 12 Admin Panel) give Site Managers/Supervisors access to this spreadsheet. Credentials stay in Script Properties only. Compromise: `refreshExotelConfigStatus()` (`src/Phase3ExotelConfigStatus.gs`) writes a non-secret `Exotel_Config_Status` tab listing only property NAMES and whether each is set (YES/NO) — never the values | Accepted |
| 2026-08-09 | `ExotelProvider.getTemplates()` is now **live-confirmed** against the user's real account (WABA `1359198589697291`, two real approved templates returned). Confirmed: endpoint is `GET /v2/accounts/<sid>/templates?waba_id=<waba_id>` (not `/whatsapp/templates` — an earlier assumption that 404'd), `waba_id` is a mandatory query parameter (Exotel returns a clear `"WabaID is mandatory"` error otherwise, not silently ignored), and the full response envelope shape (see `src/Phase3ExotelProvider.gs` inline comment). `sendText`/`sendMedia`/`sendTemplate`/`createTemplate`/`getMessageStatus` remain unverified — verifying those means actually sending a real message or creating a real template (real cost/effect), deliberately deferred to Phase 6 rather than tested speculatively now | Accepted |
| 2026-08-09 | User populated 8 of 10 numbers' `providerAccountId`/`wabaId`/`providerNumberId` (all share `providerAccountId: 'echt61'`) by editing the `WhatsApp_Numbers` sheet directly rather than through `updateNumber()`. This bypassed the audit log (no `number.updated` entries for these edits) — acceptable now since the user is the sole admin, but a reminder that once more users have write access, data changes should go through the authorized endpoint so the audit trail stays complete. `Spreewalk - Raipur` and `ECHT Advisory` still have blank provider fields, to be filled in later | Accepted |
| 2026-08-09 | Phase 4's webhook (`doPost` in `src/Phase4Webhook.gs`) is authenticated with a shared secret token in the URL (`?token=...`, Script Property `WEBHOOK_SECRET_TOKEN`), not Phase 1's `AccessControl` — Exotel is not a Google Workspace identity, so `Session.getActiveUser()`-based checks don't apply to webhook calls. Chosen over researching Exotel's own signature-verification scheme (unconfirmed whether it exists) for simplicity and to avoid another research/verification cycle | Accepted |
| 2026-08-09 | The webhook gets its own dedicated Web App deployment (`Execute as: Me`, `Access: Anyone`) rather than reusing/reconfiguring `Test_V01` — keeps `Test_V01` (`Anyone within ECHT`) untouched and reserved for a future admin-panel UI, mirroring how the Execution API got its own deployment in Phase 1 | Accepted |
| 2026-08-09 | `Phase4Api.ingestInboundMessage` also handles status-callback-shaped payloads (delivery receipts for messages sent in Phase 6), branching on `normalized.direction !== 'INBOUND'`, since Exotel delivers both event types to the same webhook URL. Applies the status update to the matching message by `providerMessageId` if found, otherwise a harmless no-op — this is the only Phase-6-adjacent logic Phase 4 does, kept minimal rather than building any outbound-sending awareness | Accepted |
| 2026-08-09 | The webhook deployment required two live fixes: (1) `appsscript.json` needed an explicit `webapp` block — without one, `clasp deploy` only offered an "API executable" entry point (no HTTP-reachable URL at all), confirmed by checking Manage Deployments in the Apps Script UI; (2) `webapp.access: "ANYONE"` maps to "Anyone **with a Google Account**" (still requires Google sign-in) — the correct value for a truly public, unauthenticated endpoint Exotel can POST to is `"ANYONE_ANONYMOUS"`, which the UI displays simply as "Anyone". Both caught by visually checking Manage Deployments before pointing Exotel at the URL, not assumed | Accepted |
| 2026-08-09 | Discovered while managing deployments: `Test_V01` is now **archived** and `Test_V02` is the **active** one (previously flagged as an unexplained deployment) — origin of `Test_V02` still unconfirmed by the user, but it's now clear it's the currently-active deployment, not a stray unused one | Accepted |
| 2026-08-10 | Added `Webhook_Debug_Log` sheet tab (`logWebhookDebug_` in `src/Phase4Webhook.gs`) logging every `doPost` call's params/body/outcome — kept as a permanent lightweight diagnostic (not a temp file), since checking a spreadsheet proved far easier than the Apps Script Executions panel for calls that didn't originate from the editor's Run button | Accepted |
| 2026-08-10 | `ExotelProvider.processWebhook()` is now **live-confirmed** against a real inbound WhatsApp message via Exotel's WhatsApp Console webhook. Two corrections from the original best-effort guess: (1) the message id field is `sid`, not `id`/`message_sid`; (2) `Phase4Api`'s number lookup needed to change entirely — Exotel's webhook `to` field is the actual E.164 phone number (e.g. `+917948502801`), not the `providerNumberId` (Meta "Phone Profile" ID) captured during Phase 3 registration. Numbers are now matched by comparing the last 10 digits of `phoneNumber` against the incoming value (`normalizePhoneTail_` in `src/Phase4Services.gs`), absorbing formatting differences between our stored `"079-485-02801"` style and Exotel's `"+917948502801"`. Also confirmed: `profile_name` carries the sender's real WhatsApp display name, now used to seed a new customer's `name` field | Accepted |
| 2026-08-10 | **Phase 4 fully live-verified end-to-end**: a real WhatsApp message ("Hola" from a real number) correctly created a `Customer` (name "Eva" from `profile_name`), an `OPEN` `Conversation` linked to the right registered number, and an `INBOUND` `Message` — all visible in the live spreadsheet. Status-callback handling (`applyStatusUpdate_`) remains unverified live, since triggering one requires actually sending a message, which is Phase 6's job | Accepted |
| 2026-08-10 | Deleted the unexplained `Test_V02` deployment (`AKfycbxbvO6...`) — user confirmed it's safe to remove ("just a demo from Exotel"). The pre-existing ngrok callback URL on all 10 numbers in Exotel is also confirmed safe to remove, left as a manual cleanup item since it's per-number config in Exotel's dashboard, not something scriptable from this repo | Accepted |
| 2026-08-10 | Phase 5's `Conversations` entity has no `teamId` field, but `AccessControl.requireConversationOperation` needs one for SUPERVISOR/SITE_MANAGER team-scoped checks. Resolved by deriving team scope from number access on the fly (`Phase5Api.resolveTeamIdForNumber_`) — a Supervisor/Site Manager can view a conversation if its `numberId` is covered by a team they're an active member of / own (`Team_Members.numberIds`), computed per request rather than adding `teamId` to the schema | Accepted |
| 2026-08-10 | Confirmed via close reading of `Phase1AccessControl.gs`: every non-admin role, including SUPERVISOR/SITE_MANAGER, needs an explicit `numberAccess` grant before anything else is checked (`hasGrantedNumber_`). Team `numberIds` is a separate, additional scope only for the team-view path. This makes `listMyNumbers()` uniform across all non-admin roles — no team-derivation needed just to decide which numbers appear in the picker | Accepted |
| 2026-08-10 | `Phase5Api.listConversations` gates on basic number access **before** filtering conversations, matching `AccessControl`'s own tamper-protection pattern (explicit `FORBIDDEN` for a completely inaccessible `numberId`, not a silently empty list) — caught by a test that expected a throw and got an empty array instead, revealing the per-conversation `try/catch` alone wasn't enough | Accepted |
| 2026-08-10 | `.claspignore` excluded `frontend/**` entirely (set up in Phase 0 when the folder was just a placeholder) — removed now that Phase 5 actually needs to push `frontend/Index.html`. Worth remembering this could have silently blocked frontend deploys with no error if not caught before the first push | Accepted |
| 2026-08-10 | Phase 5 UI deliberately omits stage/remarks/reminder panels even though the roadmap's mockup shows them — those entities exist in storage (Phase 2) but have no service layer until Phase 8/9. Showing non-functional placeholder UI was ruled out as a half-finished implementation | Accepted |
| 2026-08-10 | `phase5-admin-ui` deployment: `Execute as: Me`, `Access: Anyone within ECHT`. Initially assumed `Execute as: User accessing the web app` would be needed, but confirmed `Execute as: Me` is architecturally correct here — with `User accessing the web app`, every agent would need direct Google Sheets edit access (since `SpreadsheetApp` calls would run under their own Google authority), letting them bypass `AccessControl` entirely by opening the Sheet directly. `Execute as: Me` keeps direct Sheet access restricted to the developer only; `Session.getActiveUser()` still correctly reports each visitor's real identity because access is domain-restricted (not fully anonymous), so per-user role checks still work correctly — **live-verified 2026-08-10**: three-pane UI loaded all 10 numbers, the real conversation, and the real "Hola" message from Phase 4's test | Accepted |
| 2026-08-10 | User authorized working autonomously through subsequent phases overnight, with the explicit boundary: any action requiring their Google sign-in, real money (sending an actual WhatsApp message, creating a real Meta template), or their own business decisions (Zoho credentials, production go-live) is built/tested in code but **not executed live** — queued instead as an explicit task for them, since those carry real-world cost/consequences a blanket "go ahead" shouldn't be read to cover unsupervised | Accepted |
| 2026-08-10 | Fixed a real gap in `AccessControl.requireConversationOperation` (`src/Phase1AccessControl.gs`, needed for Phase 6): the `'reply'` and `'reassign'` action branches had no ADMIN bypass, unlike `'view'` — meaning, as originally coded, ADMIN could not reply to or reassign a conversation without being the assigned agent / having a resolvable teamId. This was clearly an oversight from Phase 1 being built before concrete conversation data existed (same class of gap as Phase 5's `teamId` discovery), not an intentional restriction — ADMIN has global scope everywhere else in the permission matrix (`docs/REQUIREMENTS.md`). Fixed with an explicit ADMIN early-return in both branches, matching `'view'`'s existing pattern | Accepted |
| 2026-08-10 | Promoted `resolveTeamIdForNumber` from a private `Phase5Api` method into `AccessControl` itself (`src/Phase1AccessControl.gs`) — Phase 6 needed the identical logic, and the pattern will recur for every future phase touching conversation-level team scope (Phase 7 reassignment, Phase 8 remarks, Phase 9 reminders). Kept as a public method on `AccessControl` since it's fundamentally authorization-scope resolution, not business logic | Accepted |
| 2026-08-10 | `Phase6Api.sendReply` uses the E.164 phone number (`toE164_`) as the "from" identifier for `ExotelProvider.sendText`, not `providerNumberId` — consistent with Phase 4's live finding that Exotel identifies numbers by phone number, not the Meta "Phone Profile" ID captured in Phase 3. This is a reasoned extrapolation, not itself live-verified — `sendText` has never actually been called against a real account (deliberately; that's a real send with real cost, deferred to when the user is present) | Accepted |
| 2026-08-10 | A `FAILED` outbound message (provider/network error) is still recorded (for visibility/retry), but `needsResponse` is only cleared on `SENT` — a failed send shouldn't make a conversation look handled | Accepted |
| 2026-08-10 | Added `lastAssignedUserId` to `Number_Assignment_Config` (Phase 2's schema, unused until Phase 7) to track the round-robin pointer. Rotation self-heals if the pointer references a user no longer in the current eligible list (starts from the front) rather than tracking drift explicitly | Accepted |
| 2026-08-10 | Phase 4's `ingestInboundMessage` now calls `Phase7Api.assignConversation` for brand-new conversations, but strictly **after** its own `LockService` lock is released, not nested inside it — `Phase7Api.assignNewLead_` acquires its own lock, and nesting two locks from one execution risks a deadlock. This completes the "Assignment" step the Phase 4 flow diagram always showed but Phase 4 itself deliberately left as a no-op | Accepted |
| 2026-08-10 | Reassignment UI is deferred to Phase 12 (Admin Panel) rather than added to `frontend/Index.html` now — it needs a user-listing endpoint properly scoped for SUPERVISOR/SITE_MANAGER too (today's `listUsers` is ADMIN-only), and Phase 12 will need that endpoint regardless. `reassignConversation` and `listAssignmentHistory` exist and are tested at the API level | Accepted |
| 2026-08-10 | **Discovered a real constraint**: `SheetRepository` has no safe header-migration mechanism — `appendRow_`/`writeRow_` build row values positionally from the current `Phase2Schemas` array, so adding a field to an entity that already has real live data (`Customers`, `Conversations` both do, from Phase 4/7 testing) would misalign already-written rows' columns. Rather than build and blind-test a migration mechanism overnight with no way to verify it against the real spreadsheet, new "attached" data for entities with live data gets its **own new tab** instead (e.g. Phase 8's `Customer_Stage` rather than a `stageId` column on `Customers`). A proper migration mechanism would be worth building later, with the user present to verify it live | Accepted |
| 2026-08-10 | Phase 8's two-tier stage authorization: managing the stage *definitions* list requires `SETTINGS_MANAGE` (ADMIN-only in practice — no other role's permission list includes it); *setting* a customer's current stage requires `LEAD_STAGES_MANAGE` (ADMIN/SITE_MANAGER/AGENT per Phase 1's existing role definitions) plus, for non-ADMIN, having at least one viewable conversation with that customer (reuses `requireConversationOperation`) — otherwise any agent could arbitrarily change any customer's stage. This scoping wasn't specified anywhere and needed a judgment call, same class of gap as the reply/reassign fixes earlier tonight | Accepted |
| 2026-08-10 | Remarks: adding requires `REMARKS_MANAGE` (ADMIN/SITE_MANAGER/AGENT) plus conversation-view access; listing accepts either `REMARKS_VIEW` (SUPERVISOR, read-only) or `REMARKS_MANAGE`. VIEWER has neither and sees no remarks at all — consistent with remarks being internal notes, not customer-facing or general read-only content | Accepted |
| 2026-08-10 | Snooze modeled with its own `Conversation_Snooze` tab (same "don't migrate a live schema" rationale as Phase 8's `Customer_Stage`) and auto-expires by timestamp comparison at read time rather than a scheduled Apps Script time trigger — simpler, no new trigger infrastructure, and avoids over-building for a need that doesn't exist yet (nothing in the roadmap asks for scheduled/background jobs) | Accepted |
| 2026-08-10 | `listReminders` only requires conversation-view access (not `REMINDERS_MANAGE`) — VIEWER can see reminders read-only even without managing them, since no `REMINDERS_VIEW`-specific permission exists in Phase 1's vocabulary (unlike remarks, which does have a VIEW/MANAGE split) and the roadmap doesn't say otherwise | Accepted |
| 2026-08-10 | Reminder/snooze actions both gate on `REMINDERS_MANAGE`, which SUPERVISOR has too (unlike remarks, where Supervisor is view-only) — matches the roadmap's explicit "Supervisor: Manage reminders" capability listed for that role | Accepted |
| 2026-08-10 | Refactored `Phase6Api` to extract a shared `sendOutbound_` helper (auth + number/customer lookup + message record + SENT/FAILED handling) before adding Phase 10's `sendTemplateReply`, rather than duplicating that whole block a second time for templates | Accepted |
| 2026-08-10 | `TEMPLATES_MANAGE` (create/edit/submit/sync a template) is effectively ADMIN-only — no other role's permission list includes it, matching the roadmap's "Admin Review" step in the template workflow. AGENT only gets `TEMPLATES_USE`, for sending an already-`APPROVED` template, not authoring one | Accepted |
| 2026-08-10 | `submitTemplateForReview` (creates a real template on the user's WABA, pending Meta review) and `sendTemplateReply` (a real send, same as Phase 6's `sendText`) are both built and Node-tested but **not invoked live** unattended — same real-world-cost boundary as everything else this session. `syncTemplatesFromProvider` reuses the already-live-confirmed `getTemplates()` call from Phase 3, so its response-parsing path is trustworthy even though the sync/upsert logic itself hasn't been run live yet (still needs a live Apps Script execution this session can't perform) | Accepted |
| 2026-08-10 | Phase 11 media modeled with its own `Message_Media` tab (same "don't migrate a live schema" rationale as `Customer_Stage`/`Conversation_Snooze`), written from both directions: `Phase6Api.sendMediaReply` (outbound) and `Phase4Api.ingestInboundMessage` (inbound, only when `normalized.mediaUrl` is present). `ExotelProvider.sendMedia` was already unverified since Phase 3; the new `extractInboundMediaUrl_` helper is equally unverified — no real inbound media webhook has ever arrived (only inbound text, confirmed 2026-08-10), so it's modeled on WhatsApp Cloud API's `content.<type>.link`/`.url` convention and flagged clearly for correction once a real one does | Accepted |
| 2026-08-10 | Quick replies reuse Phase 8's exact two-tier stage pattern: managing the list is `SETTINGS_MANAGE` (ADMIN-only), listing is open to any authenticated user. The UI inserts a selected quick reply's text into the compose textarea rather than sending immediately, so it rides the existing `sendReply` path with no new send code — deliberately not a separate "send quick reply" endpoint | Accepted |
| 2026-08-10 | `sendMediaReply` was not invoked live, same real-world-cost boundary as `sendText`/`sendTemplateReply` — deferred to the user, queued in `PROGRESS.md`'s wake-up task list | Accepted |
| 2026-08-10 | Phase 12 Admin Panel served as a **second HTML page** (`frontend/Admin.html`, routed via `?page=admin` in the existing `doGet`) rather than folding admin sections into `frontend/Index.html` — the inbox and the admin surface are different jobs for different audiences, and one 350-line file was already large enough. Reuses the same web app deployment (`phase5-admin-ui`), no new deployment needed | Accepted |
| 2026-08-10 | Added `whoAmI()` (`src/Phase1Services.gs`) — the signed-in user's own id/roleKeys — purely so the client can decide what to *show* (the Admin Panel link, an access-denied message). It is never the actual authorization boundary; every Admin Panel action still goes through its own server-side `AccessControl.require(...)` check regardless of what the client believes | Accepted |
| 2026-08-10 | Assignment Rules (`Number_Assignment_Config`/`Number_Assignment_Users`) admin CRUD gated on `NUMBERS_ADMIN` — the same permission already gating `WhatsApp_Numbers` CRUD (Phase 3), since configuring how a number assigns leads is the same class of admin operation as configuring the number itself. `setNumberAssignmentConfig` is an upsert (no separate create/update split) since the UI shouldn't need to know whether a config row already exists for a given number | Accepted |
| 2026-08-10 | `Phase7Api.listAssignableUsers(numberId)` closes the reassignment-UI gap explicitly deferred from Phase 7: ADMIN sees every active user, SUPERVISOR/SITE_MANAGER see active members of the team scoped to that number (reusing `resolveTeamIdForNumber` + `requireTeamOperation(CONVERSATIONS_REASSIGN_TEAM, ...)`), anyone else is denied. Wired into the main inbox (not the Admin Panel) since reassignment is a per-conversation inbox action, not admin configuration | Accepted |
| 2026-08-10 | Phase 12's "Dashboard" section is deliberately just current-state counts (numbers/users/open/unassigned/needs-response), not Phase 14's trend/response-time analytics — keeps this phase from stepping on a later one's scope, per the roadmap's "do not implement a future phase's functionality" rule | Accepted |
| 2026-08-10 | Phase 13's "Notifications" scoped deliberately narrow: an in-UI needs-response count badge per number (`getNeedsResponseCounts`), not push/email/SMS. Apps Script has no clean push channel, and building one (time-driven triggers, email quota management, per-user subscription state) is real new infrastructure the roadmap never asked for explicitly — a badge delivers the actual underlying need ("know what needs attention") without that cost | Accepted |
| 2026-08-10 | `Phase13Api.searchConversations` composes `Phase5Api.listMyNumbers()`/`listConversations()` rather than re-implementing any authorization logic — search/filtering only ever operates within the conversation set Phase 1's `AccessControl` (via Phase 5) already decided the signed-in user can see. This keeps exactly one place owning "who can see what," consistent with the project's centralized-authorization rule | Accepted |
| 2026-08-10 | Phase 14 dashboard metrics gated on `REPORTS_VIEW`, a permission Phase 1 already defined but nothing had used until now (SUPERVISOR/SITE_MANAGER/VIEWER/ADMIN have it, AGENT doesn't). It's a flat, non-team-scoped permission in Phase 1's existing model, so a SUPERVISOR/SITE_MANAGER with it sees **org-wide** metrics, not just their own team's — used as already defined rather than inventing a team-scoped variant unprompted, per rule #12 (don't redesign previously approved authorization without approval). Flagged in `PROGRESS.md` in case the user wants it scoped down later | Accepted |
| 2026-08-10 | **Discovered a real functional gap while building Phase 14**: no phase has ever added a way to close/resolve a conversation — `Conversations.status` is only ever written as `'OPEN'` (by Phase 4). Rather than invent a close-conversation workflow under an analytics phase (scope creep — Phase 14 is "Dashboard & Analytics," not conversation lifecycle), the `resolved` metric honestly reports `0` and the gap is flagged in `PROGRESS.md` for the user to decide whether/when a close/resolve action should be added | Accepted |
| 2026-08-10 | Template usage (Phase 14) is parsed from `Messages.messageText`'s `"[Template: name]"` marker (set by `Phase6Api.sendTemplateReply` since Phase 10) rather than adding a `templateId` column to `Messages` — same live-data-schema-safety constraint as every other "don't add a column to a live table" decision this project has made | Accepted |
| 2026-08-10 | The Reports link/overlay in `frontend/Index.html` is shown based on whether a probe call to `getDashboardMetrics()` succeeds, not a client-side role check — keeps the "who sees what" decision entirely server-side (same pattern as remarks/reminders panels hiding on `FORBIDDEN`), and doesn't require the client to know which roles happen to include `REPORTS_VIEW` | Accepted |
| 2026-08-10 | Phase 15's audit-event requirement turned out to be almost entirely already satisfied by every prior phase's own `audit_.write(...)` calls — no new logging code was needed, just a mapping documented in `docs/DATABASE.md`. No LOGOUT event exists or is planned: auth is Google's own session cookie, not an app-controlled session, so there's nothing to log a logout against | Accepted |
| 2026-08-10 | Backup (`Phase15Api`) added two new OAuth scopes to `appsscript.json` (`drive.file`, `script.scriptapp`) — a real, deliberate manifest change (not incidental) that will show the user a fresh consent screen next time the script runs. Chose `drive.file` (least-privilege — only files the script itself creates/opens) over the broader `drive` scope, even though it means there's no way to list past backups by searching Drive (that would need broader read access); the backup's Drive URL is returned directly from `backupNow()` instead | Accepted |
| 2026-08-10 | `runScheduledBackup()` (the time-driven trigger handler) calls `Phase15Api.performBackup_(null)` directly, bypassing `AccessControl` entirely — a time-driven trigger has no Google Workspace identity in its execution context, so `access_.require(...)` would simply fail every time it fired. Same system-operation reasoning already established for Phase 4's webhook ingestion (`actorUserId: null` in the audit entry) | Accepted |
| 2026-08-10 | **Real security bug found and fixed during Phase 16's QA audit**: `Phase8Api.getCustomerStage(customerId)` (`src/Phase8Services.gs`) had zero authorization check — no `currentUser()`, nothing — reachable directly via `getCustomerStage` in `src/Phase8Endpoints.gs`, letting any signed-in Google account (even one with no `Users` record at all) read any customer's lead stage regardless of relationship. Fixed to require ADMIN or the same viewable-conversation relationship (`canSeeCustomer_`) that `setCustomerStage` already enforced — read access now mirrors write access instead of having none at all. A full manual audit of every other `PhaseNApi` method (dispatched to a background agent) found no other gap of this kind; the two legitimate exceptions (`Phase4Api.ingestInboundMessage`/`applyStatusUpdate_`, `Phase15Api.performBackup_`) are never reachable via `google.script.run` in the first place — no `Phase4Endpoints.gs` file exists, and `performBackup_` is only called by the ADMIN-gated `backupNow()` or the trigger handler | Accepted |
| 2026-08-10 | Added `tests/authorization-sweep-verification.js` — a static (non-runtime) regression guard that parses every public endpoint and confirms its underlying method references `this.access_`, directly or via a same-class helper or a delegated `PhaseNApi` field. It's a heuristic, not a proof (can't verify a *delegated* class's own methods, relies on the manual audit + that class's own tests for that), but it directly protects against the exact mistake `getCustomerStage` made from recurring silently on a future endpoint | Accepted |
