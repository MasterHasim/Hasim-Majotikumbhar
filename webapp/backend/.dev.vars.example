# Copy to .dev.vars for local `wrangler dev` only — .dev.vars is gitignored,
# never commit real values. In production these are set with
# `wrangler secret put <NAME>` instead (see README.md).

# The full contents of a Firebase service account JSON file, as one line —
# generate a fresh one for this backend (Firebase Console -> Project Settings
# -> Service accounts -> Generate new private key) rather than reusing the
# Apps Script build's, so the two systems share nothing.
FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}

# Public/safe — same value as the Apps Script build's FIREBASE_WEB_API_KEY.
FIREBASE_WEB_API_KEY=

# Realtime Database URL for the service account above — e.g.
# https://<project-id>-default-rtdb.<region>.firebasedatabase.app
FIREBASE_DATABASE_URL=

# The one identity allowed to call POST /api/bootstrap and become the first
# ADMIN user — same role Apps Script's wap.phase1.bootstrapAdminEmail played.
BOOTSTRAP_ADMIN_EMAIL=

# Comma-separated list of origins allowed to call this backend (see lib/cors.ts).
# Include http://localhost:5173 for local frontend dev.
ALLOWED_ORIGINS=http://localhost:5173

# The deployed frontend's origin — used to build links in outbound emails/WhatsApp
# messages (e.g. "Sign in at ...").
FRONTEND_URL=http://localhost:5173

# Exotel WhatsApp API credentials (needed once we port the send/webhook phase).
EXOTEL_API_KEY=
EXOTEL_API_TOKEN=
EXOTEL_ACCOUNT_SID=
EXOTEL_SUBDOMAIN=

# Shared secret Exotel's webhook must present (generate a new random value —
# don't reuse the Apps Script one, keep the two systems fully independent).
WEBHOOK_SECRET_TOKEN=

# Meta Graph API long-lived user or System User token — powers Dashboard's Ad
# Performance report and the Leads "Live Campaign" dropdown. Optional: both
# features soft-fail to empty/no-op without it.
META_ACCESS_TOKEN=

# Resend (transactional email) — optional: welcome emails silently no-op without it.
RESEND_API_KEY=
RESEND_FROM_EMAIL=

# Zoho CRM Contact sync. Keep all three OAuth values server-side only (Worker secrets in
# production). Set ZOHO_ACCOUNTS_URL to your data centre, e.g. https://accounts.zoho.in.
# The external-id field must already exist on Zoho Contacts and be configured as unique.
ZOHO_CLIENT_ID=
ZOHO_CLIENT_SECRET=
ZOHO_REFRESH_TOKEN=
ZOHO_ACCOUNTS_URL=https://accounts.zoho.com
ZOHO_CONTACT_EXTERNAL_ID_FIELD=Echt_Customer_ID

# Development-only Zoho Function test path. Keep the complete Function URL (including any
# API-key query value) in this secret; never put it in frontend/.env*. It remains disabled until
# the Function's request contract is documented and explicitly verified.
ZOHO_TEST_FUNCTION_URL= https://www.zohoapis.in/crm/v7/functions/dashboardreq/actions/execute?auth_type=apikey&zapikey=1003.d58c0e20866dd5d85ad6a7ee7382871f.4dbdcdc01a927bc66f4ac9ca3bbff5b6
ZOHO_TEST_FUNCTION_CONTRACT_VERIFIED=false
# Optional: export only explicitly-approved Customer/Lead custom-field keys, comma-separated.
ZOHO_TEST_CUSTOM_FIELD_ALLOWLIST=expected_revenue

# R2 bucket binding is configured in wrangler.toml, not here — see [[r2_buckets]].
