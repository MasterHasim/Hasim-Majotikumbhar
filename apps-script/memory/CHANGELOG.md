# Changelog

## 2026-08-17 (follow-up 8)
- Proactive audit after the two `=== true`/`getUrl()` bugs found this session: swept the codebase for the same `=== true` fragility on other Sheets-backed tables. Fixed `Location_Assignment_Users.active === true` → `!== false` in `selectNextLocationAgent_` (`src/Phase22Services.gs`) — my own table, same latent risk as the `WhatsApp_Numbers` bug if ever hand-edited in the sheet. Left `Phase7Services.gs`'s identical pattern on `Number_Assignment_Users.active` alone — same latent risk, but that's pre-existing, live-verified, production code outside this session's scope; flagged for the user to decide rather than silently changed. All 24 suites pass.

## 2026-08-17 (follow-up 7)
- **Real bug found via live testing**: "Send WhatsApp" on a Raipur lead failed with "No WhatsApp number is configured for location 'Raipur'" even though "Entartica - Raipur" clearly existed and was selected in the sidebar. Root cause: `findNumberForLocation_` (`src/Phase22Services.gs`) filtered on `n.active === true` (strict), but `WhatsApp_Numbers` rows have been hand-edited directly in the sheet before (memory/DECISIONS.md) — a manually-typed "TRUE" round-trips through the plain-text-formatted `active` column as the *string* `"TRUE"`, not a boolean, so the strict check silently excluded every number. Fixed to `!== false`, matching the same lenient convention `Phase3Services.gs`/`Phase11Services.gs` already use for exactly this reason. Added a regression test using `active: 'TRUE'` (string) to catch this class of bug going forward. All 24 suites pass.

## 2026-08-17 (follow-up 6)
- Collapsed the Users table's two password-recovery buttons ("Send setup link" + "Generate temp password") into one **Reset password** button, user-directed — email-a-reset-link only, no temp password shown to the admin at all. Removed the now-dead `submitSetTemporaryPassword` frontend function; the `setTemporaryPassword` backend endpoint is left intact/unused rather than deleted, in case a no-email fallback is needed again later. "Add user"'s combined create+welcome-email flow is unchanged (separate feature).

## 2026-08-17 (follow-up 5)
- **Real bug found via live testing**: a "Send setup link" email 403'd for the recipient — the URL was `.../dev?resetToken=...` instead of `.../exec?resetToken=...`. `ScriptApp.getService().getUrl()` returns whatever deployment is serving the *current* request, so it captured a `/dev` test-deployment URL the admin happened to be browsing from at the time. Added `webAppUrl_()` (`src/PasswordAuthDomain.gs`) preferring a new `WEB_APP_URL` Script Property over `getUrl()`; updated both `sendWelcomeEmail` and `sendResetEmail_` (`src/PasswordAuthServices.gs`) to use it. All 24 suites pass.

## 2026-08-17 (follow-up 4)
- Connected Leads to Lead Stages, Comments, WhatsApp send, and Inbox calling, user-directed. New `Lead_Stage_Assignment`/`Lead_Remarks` tabs + repositories (mirroring `Customer_Stage`/`Remarks` exactly). New `Phase22Api` methods: `setLeadStage`/`getLeadStage` (reuses Phase 8's existing `Lead_Stages` list, no separate stage set), `addLeadRemark`/`listLeadRemarks`, `startWhatsAppFromLead` (resolves a lead's location to a WhatsApp Number by displayName substring match, find-or-creates a Customer+open Conversation assigned to the calling agent, checks `numberAccess` first), `initiateConversationCall` (click-to-call from the regular Inbox, using `requireConversationOperation('reply', ...)` as the gate and that conversation's own WhatsApp number as caller ID). All six reuse Phase 1's original permissions (`LEAD_STAGES_MANAGE`/`REMARKS_MANAGE`/`REMARKS_VIEW`/`LEADS_CALL`) — no new permission tokens, so no second role-permissions fixup needed.
- New endpoints registered in both `Phase22Endpoints.gs` and `PASSWORD_AUTH_API_ALLOWLIST_` (`src/PasswordAuthEndpoints.gs`) — `authorization-sweep-verification.js` now covers 90 endpoints (up from 84), all still access-checked.
- Frontend: new Lead Detail modal (`#leadDetailModalOverlay`) reachable from both All Leads and My Leads tables — stage dropdown, comments list + add box, Call and Send WhatsApp buttons. "Send WhatsApp" closes the modal, switches to the resolved number's workspace if needed, and opens the conversation directly in the existing Inbox UI. Added a Call button next to the phone field in the regular conversation detail panel (`renderDetailPanel`).
- Extended `tests/phase22-leads-verification.js` with Firebase mocking (Conversations/Customers path needed it, unlike the original Leads-only tests) and coverage for: stage set/get + non-owner denial, comment add/list + non-owner denial, WhatsApp conversation creation (including numberAccess-required and reused-existing-open-conversation cases), a clear `CONFIGURATION_ERROR` when a location has no matching WhatsApp number yet, and Inbox click-to-call using the conversation's own number as caller ID + non-assignee denial. All 24 backend suites pass.

## 2026-08-17 (follow-up 3)
- **Real bug found via live testing**: after deploying Phase 22, the user hit `Phase1Error: Missing permission: leads.manage` as ADMIN. Root cause: `Phase1RoleDefinitions` in code only seeds Role records at `bootstrapPhase1()` time — this project bootstrapped once, back on 2026-08-09, and existing Role records carry a fixed `permissions` snapshot from then. Adding `LEADS_MANAGE`/`LEADS_VIEW_ASSIGNED`/`LEADS_CALL` to the code doesn't retroactively update already-persisted roles. No `updateRole` endpoint exists to fix this through the app. Added `src/Phase22RolePermissionsFixupTemp.gs` (`fixupPhase22RolePermissions()`), a one-time idempotent fixup adding the three new permissions to the existing ADMIN/SITE_MANAGER/AGENT role records — same temp-wrapper-run-once-via-editor convention as the original bootstrap (`clasp run` doesn't work on this auto-managed GCP project). To be deleted after the user runs it once.
- **Also found via this debugging**: the first two `clasp push` calls (turn "Yes, push it" x2) reported success but genuinely did not update the live script — confirmed by pulling the live content back down and finding no Phase 22 files present. A third push succeeded and was independently verified the same way. Root cause not fully diagnosed (clasp/Apps Script API flakiness, not a code issue); worth verifying future pushes with a pull-back check rather than trusting `clasp push`'s own success message alone.

## 2026-08-17 (follow-up 2)
- User confirmed the full location→ExoPhone mapping: Raipur 02804, Rajsamand 02805, Prayagraj 02806, Saraighat 02807, Coimbatore 02808, Alibaug 02809. Added `Phase22Validation.callerId` (strips spaces/dashes, no strict length check — landline ExoPhones vary) so pasting straight from a "079-485-02804"-style display table into the admin Caller ID field doesn't send dashes to Exotel's API. `setLocationConfig` now normalizes on save. Extended `tests/phase22-leads-verification.js` to assert the dash-stripping. All 24 suites pass.

## 2026-08-17 (follow-up)
- Added per-location caller ID support to Phase 22: `Location_Assignment_Config` gained a `callerId` column, `ExotelVoiceProvider.connectCall` takes an optional third `callerId` argument (falls back to `EXOTEL_VOICE_CALLER_ID` when omitted), `Phase22Api.initiateCall` passes the lead's location's caller ID through, and the Assignment Rules admin panel gained a Caller ID input per location. Driven by the user having a separate ExoPhone per brand/location rather than one shared caller ID. Extended `tests/phase22-leads-verification.js` to cover both the fallback-to-default and location-override cases. All 24 backend suites pass.

## 2026-08-17
- Implemented Phase 22 — Location Leads Upload, Assignment Rules & Exotel Click-to-Call, user-directed. Admins upload a spreadsheet of call leads (name/phone/location) for six fixed locations (Raipur, Rajsamand, Coimbatore, Prayagraj, Alibaug, Saraighat); each lead auto-assigns to a site agent per a per-location rule (single fixed agent / round robin / manual). Agents get a "My Leads" view with a click-to-call button that rings their own phone first, then connects to the lead via Exotel's Voice API.
- New tables added directly to `Phase2Schemas` in `src/Phase2Domain.gs` (matching the existing Phase 8/9/11 convention): `Leads`, `Location_Assignment_Config`, `Location_Assignment_Users`, `Call_Log`. New `src/Phase22Domain.gs` (locations/modes/status enums + validation), `src/Phase22Services.gs` (`Phase22Api` — upload/dedupe, per-location assignment mirroring Phase 7's round-robin engine, manual reassignment, role-scoped `listLeads`, location assignment-rule CRUD, `initiateCall`), `src/Phase22Endpoints.gs`, new `LeadRepository`/`LocationAssignmentRepository`/`CallLogRepository` in `src/Phase2Repositories.gs`.
- New `src/Phase22ExotelVoice.gs` (`ExotelVoiceProvider.connectCall`) — separate credentials/domain from the existing WhatsApp `ExotelProvider`, flagged UNVERIFIED pending a real test call.
- Added three permissions to Phase 1's role matrix (`LEADS_MANAGE`: ADMIN+SITE_MANAGER; `LEADS_VIEW_ASSIGNED`/`LEADS_CALL`: AGENT) and an optional `phone` field on user records, editable from the existing Edit User modal. Registered all 11 new endpoint functions in `PASSWORD_AUTH_API_ALLOWLIST_` (`src/PasswordAuthEndpoints.gs`) — missing this would have made every new endpoint unreachable for any password-session user, caught by re-reading that dispatcher's allowlist pattern before writing the endpoints file.
- Frontend: new "Location Leads" (admin: Upload / Assignment Rules / All Leads tabs) and "My Leads" (agent: call button) sidebar sections in `frontend/Index.html`, reusing the existing Number round-robin config UI pattern for the per-location assignment-rules panel and the existing reusable Edit modal for manual reassignment.
- New `tests/phase22-leads-verification.js` (Node-mocked `SpreadsheetApp`/`UrlFetchApp`/`LockService`): upload+dedupe, invalid-row handling, single/round-robin/manual assignment modes (round-robin wrap-around verified the same way `phase7-assignment-verification.js` verifies Phase 7's), manual reassignment, AGENT-scoped `listLeads`, `LEADS_MANAGE` permission denial for AGENT, click-to-call success and cross-agent denial. All 24 test suites pass, including the unmodified `authorization-sweep-verification.js` (now covering 84 endpoints, up from 73) and `phase7-assignment-verification.js` (confirms the shared `Phase1Domain.gs`/`Phase1Services.gs`/`Phase2Domain.gs` edits didn't regress existing behavior).
- Not yet done (flagged for the user): Exotel Voice Script Properties (`EXOTEL_VOICE_ACCOUNT_SID`/`_API_KEY`/`_API_TOKEN`/`_CALLER_ID`) are unset — `initiateCall` will throw `CONFIGURATION_ERROR` until set. Each agent's own phone number needs entering once via the Edit User modal. `connectCall()`'s request/response shape needs a real test call to confirm before trusting it in production.

## 2026-08-08
- Created initial project foundation.
- Created memory and documentation structure.
- Added baseline Git / CLASP project files.
- Implemented Phase 1 access-control foundation: authentication context, users, roles, teams and team members, number access, availability, eligibility, repositories, audit logging, and central authorization.
- Verified and enforced the approved fixed five-role Phase 1 permission matrix, including team scope, explicit number grants, eligibility, availability, and server-side authorization contracts.

## 2026-08-09
- Consolidated the master 21-phase roadmap into `docs/ROADMAP.md` (Phase 0 Foundation through Phase 21 Handover), superseding the earlier coarser phase numbering. Phase 2 is now the Core Database/Repository Layer; WhatsApp/Exotel integration moves to Phase 3.
- Configured `appsscript.json` with `executionApi.access = MYSELF` and explicit `oauthScopes` (`script.storage`, `userinfo.email`) so the Apps Script API can execute administrative functions.
- Created deployment `phase1-bootstrap-execution-api` (`AKfycbxFvVo9BtvKtP-GWT_626AH0JACKGHweKKezoBriHZ4N6tz0PeEfcjgZhmAESVsmn0nOQ`), separate from the existing `Test_V01` web app deployment.
- Executed `bootstrapPhase1({email: 'hasim@echt.co.in', displayName: 'Hasim'})` exactly once, via a temporary Apps Script editor-run wrapper (`src/Phase1BootstrapTemp.gs`, deleted immediately after use) — `clasp run` / Execution API could not authorize because the script is bound to Apps Script's auto-managed default GCP project rather than a standard one.
- Verified: Hasim is `ACTIVE` `ADMIN` (`user_68a11404-202f-4109-b52a-ead4c83e98b5`), all five fixed roles created, and the audit log contains `phase1.bootstrapped` plus the prior `authentication.denied` / `UNKNOWN_USER` entries from pre-bootstrap test calls.
- **Phase 1 is now operationally verified end-to-end.** Phase 2 (Core Database/Repository Layer) is next per `docs/ROADMAP.md`.
- Discovered an undocumented deployment `Test_V02` (`AKfycbxbvO6FMXsaRJHHDoPZFsGZVeHv0xRm1CloaBLwKpsvayXgVOtNoPhRek04Z8o4tBWpgg`) not recorded in any prior project memory — origin unconfirmed, flagged for owner review.
- Implemented Phase 2 Core Database/Repository Layer: `SheetRepository` (`src/Phase2Repository.gs`), a second persistence adapter backed by Google Sheets, conforming to the same repository contract as `PropertiesRepository`. Added eleven concrete repositories (`src/Phase2Repositories.gs`) covering `WhatsApp_Numbers`, `Number_Assignment_Config`/`Number_Assignment_Users` (via `AccessRepository`), `Customers`, `Conversations`, `Conversation_Assignments`, `Messages`, `Remarks`, `Reminders`, `Lead_Stages`, `WhatsApp_Templates`, and `Quick_Replies`, with column schemas in `src/Phase2Domain.gs` and contracts documented in `docs/DATABASE.md` and `data/schemas/phase2-contracts.json`.
- Verified with `tests/phase2-repository-verification.js` (Node-mocked `SpreadsheetApp`): CRUD round-trip, header auto-creation, `CONFLICT`/`NOT_FOUND` errors, and `CONFIGURATION_ERROR` when `wap.phase2.spreadsheetId` is unset. `tests/phase1-role-verification.js` still passes — Phase 1 files untouched.
- **Phase 2 ships storage and schema contracts only** — no services, no public endpoints, no real spreadsheet provisioned, no live Apps Script execution. Phase 3 (WhatsApp Numbers & Exotel Integration) is next, and is expected to provision the real spreadsheet.
- Opened GitHub PR #1 (`phase-2-repository-layer` → `main`); merged the same day. Added `PROGRESS.md` as the single living progress-report file, updated after every phase transition.
- User provisioned the real backing spreadsheet ahead of Phase 3: created `1qugfpq7dfNd2phwb8GVh_6VEsDe1Kf0fd76w3JQcqt4` and configured it as Script Property `SPREADSHEET_ID`. Updated `Phase2Spreadsheet.SCRIPT_PROPERTY` (was `wap.phase2.spreadsheetId`); tests and docs updated accordingly. All Phase 2 repositories can now read/write real data once services are wired to them in Phase 3.
- Live smoke test against the real spreadsheet caught a real bug: `ReferenceError: SheetRepository is not defined` when running `NumberRepository` in the Apps Script editor, even though `node tests/phase2-repository-verification.js` passed. Root cause: Apps Script concatenates `.gs` files in filename-alphabetical order, and `class NumberRepository extends SheetRepository` is evaluated at file-load time — `Phase2Repositories.gs` sorted *before* `Phase2Repository.gs` alphabetically (`"Repositories" < "Repository"`, since `i` < `y`), so `SheetRepository` didn't exist yet. The Node test hadn't caught it because it loaded files in a hand-picked safe order instead of mimicking Apps Script's real order.
- Fix: renamed `src/Phase2Repository.gs` → `src/Phase2Persistence.gs` (sorts before `Phase2Repositories.gs`); updated `tests/phase2-repository-verification.js` to load every `.gs` file in `src/` sorted alphabetically, matching Apps Script's actual behavior, so this class of bug is caught locally going forward.
- Second bug caught by the same live smoke test: `NumberRepository.create()` wrote `phoneNumber: '000'` but reading it back returned the number `0` — Google Sheets silently coerces numeric-looking strings into real numbers on write, which would corrupt phone-number-style IDs. Fixed in `src/Phase2Persistence.gs`'s `appendRow_`/`writeRow_`: applying `Range.setNumberFormat('@')` once at sheet creation, and even calling it on the exact write range via `getRange().setValues()` instead of `sheet.appendRow()`, were each insufficient on their own — only applying `setNumberFormat('@')` directly on the exact target range immediately before `setValues()`, every write, reliably prevented the coercion. Re-verified live: `readBack.phoneNumber` came back as `"000"` (string), full create→read→count→remove cycle correct.
- Deleted the temporary `src/Phase2SmokeTestTemp.gs` after verification; `appsscript.json` also gained the `https://www.googleapis.com/auth/spreadsheets` OAuth scope (was missing, caused an initial "Specified permissions are not sufficient" error).
- **Phase 2 is now fully verified end-to-end against the live spreadsheet**, not just Node-mocked. `WhatsApp_Numbers` tab exists on `1qugfpq7dfNd2phwb8GVh_6VEsDe1Kf0fd76w3JQcqt4` with correct plain-text formatting, ready for Phase 3 to populate for real.
- Implemented Phase 3 WhatsApp Numbers & Exotel Integration: `Phase3ProviderContract` and `Phase3ExotelConfig` (`src/Phase3Domain.gs`), `ExotelProvider` (`src/Phase3ExotelProvider.gs`) implementing `sendText`/`sendMedia`/`sendTemplate`/`getTemplates`/`createTemplate`/`getMessageStatus`/`processWebhook` via `UrlFetchApp`, and `Phase3Api`/endpoints (`src/Phase3Services.gs`, `src/Phase3Endpoints.gs`) exposing authorized `createNumber`/`updateNumber`/`listNumbers` over `NumberRepository` — the first real caller of a Phase 2 repository.
- Researched Exotel's public WhatsApp API docs (their detailed reference pages 404'd on fetch, likely JS-rendered); confirmed only the base URL pattern, Basic Auth scheme, and status codes (30001/30002/30003 = Sent/Delivered/Seen). Every other request/response field name in `ExotelProvider` is a flagged best-effort guess pending live verification once `EXOTEL_API_KEY`/`EXOTEL_API_TOKEN`/`EXOTEL_ACCOUNT_SID`/`EXOTEL_SUBDOMAIN` Script Properties are set.
- Verified with `tests/phase3-numbers-verification.js` (ADMIN-only authorization, duplicate-phone rejection, patch-field validation) and `tests/phase3-provider-verification.js` (Node-mocked `UrlFetchApp` — confirms *our* request construction, not Exotel's actual acceptance of it). All four test suites (`phase1`, `phase2`, `phase3-numbers`, `phase3-provider`) pass.
- Registered all 10 known WhatsApp numbers for real via a one-time `seedPhase3NumbersOnce()` wrapper (deleted after use) calling the authorized `createNumber` endpoint — all 10 succeeded, no conflicts.
- User asked about storing Exotel credentials in a sheet tab; declined per `docs/SECURITY.md`'s existing rule against secrets in Google Sheet cells, and the fact that later phases give broader spreadsheet access to Site Managers/Supervisors. Added `refreshExotelConfigStatus()` (`src/Phase3ExotelConfigStatus.gs`) instead: a non-secret `Exotel_Config_Status` tab listing which `EXOTEL_*` Script Properties are set (names only, never values). Verified with `tests/phase3-exotel-config-status-verification.js`, including an explicit assertion that no secret value ever appears in the written output.
- User set the four `EXOTEL_*` Script Properties. First live test hit `whatsapp/templates` → 404, which turned out to be a genuinely wrong path (confirmed by further research), not an auth problem — the 404 response was well-formed JSON, proving auth/base URL/subdomain/account SID were already correct.
- Fixed the path to `templates` (confirmed via a real curl example found in search results: `GET .../v2/accounts/<sid>/templates?waba_id=<waba_id>`). Second live test returned a clean, well-formed `"WabaID is mandatory"` validation error — confirming the endpoint itself was now right, just missing a required parameter.
- User located their WABA ID and Phone Number ID in Meta Business Manager (WhatsApp account entry ID vs. Phone Profile ID) and populated `providerAccountId`/`wabaId`/`providerNumberId` for 8 of 10 numbers directly in the sheet (all share `providerAccountId: 'echt61'`; `Spreewalk - Raipur` and `ECHT Advisory` still blank).
- Third live test with a real WABA ID (`1359198589697291`, the "Compliances" number) **succeeded**: returned two real, approved WhatsApp templates (`otp_veri_code`, `otp`) from the user's actual account. `getTemplates()` is now genuinely live-verified, not just Node-mocked — confirmed request path, required `waba_id` parameter, and full response envelope shape, all documented inline in `src/Phase3ExotelProvider.gs` and in `memory/DECISIONS.md`.
- **Phase 3 is done for what it set out to do**: numbers registered, provider abstraction built, `getTemplates()` live-verified end-to-end. `sendText`/`sendMedia`/`sendTemplate`/`createTemplate`/`getMessageStatus` remain unverified by design — testing those means actually sending a real message or creating a real template, deliberately deferred to Phase 6 (Agent Reply/Outbound Messaging) rather than done speculatively now. Deleted the temporary `src/Phase3ExotelSmokeTestTemp.gs` after use.
- Implemented Phase 4 Webhook & Message Ingestion: `Phase4WebhookConfig` (`src/Phase4Domain.gs`), `Phase4Api.ingestInboundMessage` (`src/Phase4Services.gs`) — idempotent (dedupes on `providerMessageId`), identifies the number/customer, finds-or-creates an OPEN conversation, stores the message, and also handles status-callback payloads for previously-sent messages. `doPost` (`src/Phase4Webhook.gs`) is the project's first HTTP entry point, authenticated with a shared secret token (`WEBHOOK_SECRET_TOKEN`) rather than Phase 1's `AccessControl`, since Exotel has no Google Workspace identity.
- Verified with `tests/phase4-ingestion-verification.js` (unknown number rejected, duplicate detection, conversation reuse, status-callback handling) and `tests/phase4-webhook-verification.js` (missing/wrong token rejected without touching data, valid payload ingested, malformed JSON caught gracefully). All seven test suites pass.
- Replaced the `docs/WEBHOOK.md` placeholder with the real flow, auth model, and idempotency design.
- Added `webapp` block to `appsscript.json` (was missing entirely — without it, deployments only offered "API executable", no HTTP-reachable URL). First attempt used `access: "ANYONE"`, which turned out to mean "Anyone with a Google Account" (still requires Google sign-in); corrected to `"ANYONE_ANONYMOUS"` after visually confirming in Manage Deployments, since Exotel has no Google identity to sign in with.
- Created dedicated deployment `phase4-webhook-ingestion` (`Execute as: Me`, `Access: Anyone`), separate from `Test_V01`/`Test_V02`. Discovered while managing deployments that `Test_V01` is now archived and `Test_V02` is the active one — `Test_V02`'s origin still unconfirmed.
- Generated a random `WEBHOOK_SECRET_TOKEN`, user set it as a Script Property and configured the webhook URL on all 10 numbers in Exotel's WhatsApp Console (Webhooks → Number Management → Edit Callback URLs) — found and left alongside a pre-existing, likely-stale ngrok tunnel URL from earlier testing rather than removing it unasked.
- Live-tested with real WhatsApp messages. First attempt failed clearly (`"providerMessageId is required"`) — the real payload's message-id field is `sid`, not `id`/`message_sid` as assumed. Fixed and retested; second attempt failed differently (`"No registered number matches"`) — revealed that Exotel's webhook `to` field is the E.164 phone number, not the `providerNumberId` (Meta Phone Profile ID) captured in Phase 3. Rewrote the number-lookup to match on normalized phone digits instead (`normalizePhoneTail_`). Third attempt **succeeded completely**: real customer (name auto-populated from WhatsApp's `profile_name`, "Eva"), real OPEN conversation, real inbound message, all correctly linked and visible in the live spreadsheet.
- Added `Webhook_Debug_Log` sheet tab (`logWebhookDebug_`) as a permanent lightweight diagnostic — checking a spreadsheet proved much easier than the Apps Script Executions panel for webhook-triggered calls.
- **Phase 4 is done, live-verified end-to-end.** Status-callback handling remains unverified live (requires actually sending a message — Phase 6's job).
- Deleted the unexplained `Test_V02` deployment after the user confirmed it (and the pre-existing ngrok webhook URL) were just Exotel demo artifacts, safe to remove.
- Implemented Phase 5 Conversations & Inbox: `Phase5Api` (`src/Phase5Services.gs`) — `listMyNumbers`, `listConversations`, `getConversationDetail` — plus `doGet` (`src/Phase5Endpoints.gs`), the project's first HTML UI, and `frontend/Index.html`, a three-pane plain HTML/CSS/JS panel using `google.script.run`.
- Resolved a real gap found during planning: `Conversations` has no `teamId`, but `requireConversationOperation` needs one for SUPERVISOR/SITE_MANAGER. Fixed by deriving team scope from number access on the fly (`resolveTeamIdForNumber_`) rather than changing the schema. Also clarified via close reading of `Phase1AccessControl.gs` that every non-admin role needs an explicit `numberAccess` grant regardless of team membership — team `numberIds` is a separate, additional scope only for the team-view path.
- Testing caught a real design gap: `listConversations` needed an explicit upfront access gate (matching `AccessControl`'s own tamper-protection pattern) — without it, a completely inaccessible number silently returned an empty list instead of `FORBIDDEN`, since the per-conversation `try/catch` swallowed the denial.
- Discovered `.claspignore` excluded `frontend/**` entirely (leftover from Phase 0, when the folder was just a placeholder) — would have silently blocked `frontend/Index.html` from ever reaching Apps Script. Fixed before the first push.
- Verified with `tests/phase5-inbox-verification.js`: ADMIN sees everything; AGENT sees only their granted number and only conversations assigned to them; SITE_MANAGER sees conversations on numbers their team covers and is denied on numbers outside it. All eight test suites pass.

## 2026-08-10 (overnight, autonomous)
- User authorized continuing through subsequent phases unattended, with an explicit boundary: nothing requiring their Google sign-in, real money, or their own business decisions gets executed live — built and tested in code, then queued as a task for them.
- Fixed a real gap in `AccessControl.requireConversationOperation` needed for Phase 6: `'reply'` and `'reassign'` had no ADMIN bypass (unlike `'view'`), meaning ADMIN couldn't reply to or reassign a conversation unless also the assigned agent / had a resolvable teamId. Fixed with explicit ADMIN early-returns, matching `'view'`'s existing pattern. Added a regression test to `tests/phase1-role-verification.js`.
- Promoted `resolveTeamIdForNumber` from a private `Phase5Api` method into `AccessControl` itself — Phase 6 needed the identical logic, and it'll recur for Phase 7 (reassignment), Phase 8 (remarks), Phase 9 (reminders). Refactored `Phase5Api` to call the shared method; no behavior change (all prior tests still pass).
- Implemented Phase 6 Agent Reply / Outbound Messaging: `Phase6Api.sendReply` (`src/Phase6Services.gs`), reusing `requireConversationOperation('reply', ...)` — ADMIN globally, AGENT only for their own assigned conversations, matching `docs/REQUIREMENTS.md`'s permission table (Supervisor/Site Manager aren't reply-capable there). Records `senderUserId`, `direction: 'OUTBOUND'`, `status: SENT`/`FAILED` (failures still recorded for visibility, `needsResponse` only clears on `SENT`). Added a compose/reply box to `frontend/Index.html`.
- `sendText`'s "from" identifier uses the E.164 phone number (`toE164_`), consistent with Phase 4's confirmed finding that Exotel identifies numbers by phone number rather than `providerNumberId` — a reasoned extrapolation, **not itself live-verified**. Sending a real message has real cost and reaches a real phone, so this was deliberately not tested live while unattended — flagged for the user.
- Verified with `tests/phase6-reply-verification.js` (Node-mocked `UrlFetchApp`, no real network calls): authorization (ADMIN/assigned-AGENT allowed, other AGENT/VIEWER denied), correct E.164 from/to in the request, `SENT` clears `needsResponse`, a simulated provider failure records `FAILED` and leaves `needsResponse` untouched. All nine test suites pass.
- Implemented Phase 7 Assignment & Round-Robin Engine: `Phase7Api` (`src/Phase7Services.gs`) — new leads round-robin across eligible participants (`Number_Assignment_Users`, filtered by active status, `numberAccess`, `assignmentEligibility`, and `availability` — four independent checks per Phase 1's design), returning customers inherit their prior owner, fallback/unassigned-queue handling, working-hours restriction, and full `Conversation_Assignments` history for every assignment (`round_robin`/`returning_customer`/`fallback`/`manual`).
- Added `lastAssignedUserId` to `Number_Assignment_Config`'s schema (Phase 2, unused until now) to track the rotation pointer — self-heals if the pointer references a no-longer-eligible user rather than tracking drift.
- Wired into Phase 4: `ingestInboundMessage` now calls `Phase7Api.assignConversation` for brand-new conversations, completing the "Assignment" step the Phase 4 flow diagram always showed. Called strictly after Phase 4's own lock releases (not nested) to avoid a two-lock deadlock risk within one execution.
- `reassignConversation` reuses the ADMIN/reassign fix from earlier in this session; `listAssignmentHistory` exposes the audit trail. Reassignment UI itself deferred to Phase 12 (Admin Panel), which will need a properly role-scoped user-listing endpoint anyway.
- Verified with `tests/phase7-assignment-verification.js`: rotation order and wraparound, skipping an ineligible participant, nobody-available → unassigned, fallback user catches that case, working-hours restriction, returning-customer inheritance (no pointer advance), manual reassignment authorization. Added an integration check to `tests/phase4-ingestion-verification.js` confirming a real ingested lead gets auto-assigned. All ten test suites pass.
- Discovered a real constraint while designing Phase 8: `SheetRepository` has no safe header-migration path — adding a field to `Customers` or `Conversations` (both already have real live data) would misalign existing rows, since row writes are positional against the current schema array. Decided to give any new "attached" data on entities with live data its own tab instead, rather than build and blind-test a migration mechanism with no way to verify it live overnight.
- Implemented Phase 8 CRM-lite: `Phase8Api` (`src/Phase8Services.gs`) — lead stage definitions (`SETTINGS_MANAGE`, effectively ADMIN-only) seeded from a documented default list (`Phase8DefaultStages`, `src/Phase8Domain.gs` — New/Contacted/Interested/Quotation Sent/Negotiation/Won/Lost, freely editable after seeding, not hardcoded logic), a customer's current stage (`Customer_Stage`, a new tab — `LEAD_STAGES_MANAGE`, plus for non-ADMIN a viewable-conversation relationship check so an agent can't set an unrelated customer's stage), and internal remarks (`REMARKS_MANAGE` to add, `REMARKS_VIEW` or `REMARKS_MANAGE` to list — VIEWER has neither).
- Added a stage dropdown and a remarks mini-panel to `frontend/Index.html`'s conversation detail pane.
- Verified with `tests/phase8-crm-lite-verification.js`: stage-definition ADMIN-only gating and seed-once idempotency, agent can set stage only for a related customer, remarks add/list authorization matrix across ADMIN/AGENT/SUPERVISOR/VIEWER. No live/costly verification needed — pure internal logic. All eleven test suites pass.
- Implemented Phase 9 Reminders, Snooze & Follow-up: `Phase9Api` (`src/Phase9Services.gs`) — reminders (`createReminder`/`updateReminderStatus`/`listReminders`/`listMyReminders`, PENDING/COMPLETED/CANCELLED, gated on `REMINDERS_MANAGE` — SUPERVISOR included this time, unlike remarks, matching the roadmap's explicit "Supervisor: Manage reminders") and snooze (`snoozeConversation`/`unsnoozeConversation`/`getSnoozeStatus`, its own `Conversation_Snooze` tab, same live-data-safety rationale as Phase 8's `Customer_Stage`). Snooze auto-expires by timestamp comparison at read time — no scheduled trigger infrastructure needed or built.
- Wired snooze-awareness into Phase 5: `listConversations` now excludes currently-snoozed conversations from the active list via a shared `isConversationSnoozed_` helper (`src/Phase9Domain.gs`), matching "temporarily leaves the active queue."
- Added a reminders mini-panel and a Snooze/Un-snooze button (with a snoozed-until banner) to `frontend/Index.html`'s conversation detail pane.
- Verified with `tests/phase9-reminders-verification.js`: reminder authorization (AGENT/SUPERVISOR allowed, VIEWER denied to create but can still list), status transitions and invalid-status rejection, `listMyReminders` scoping, snooze hiding a conversation from Phase 5's list, auto-expiry, and explicit unsnooze. No live/costly verification needed. All twelve test suites pass.
- Refactored `Phase6Api` to extract a shared `sendOutbound_` helper (auth, number/customer lookup, message record, SENT/FAILED handling) before adding template sending, avoiding duplicating that whole block a second time.
- Implemented Phase 10 WhatsApp Templates: `Phase10Api` (`src/Phase10Services.gs`) — draft/edit (`TEMPLATES_MANAGE`, ADMIN-only in practice) → `submitTemplateForReview` (real `ExotelProvider.createTemplate` call, creates a real template on the WABA pending Meta review) → `syncTemplatesFromProvider` (upserts from the already-live-confirmed `getTemplates()`, matched by `providerTemplateId` so re-syncs don't duplicate). `Phase6Api.sendTemplateReply` sends an `APPROVED` template with `{{n}}` variables substituted into its `BODY` component, records an `OUTBOUND` `messageType: 'template'` message.
- Added a template dropdown + "Send Template" button to the compose row in `frontend/Index.html`, prompting once per placeholder found in the selected template.
- `submitTemplateForReview` and `sendTemplateReply` are built and Node-tested but not invoked live — same real-world-cost boundary as `sendText`. `syncTemplatesFromProvider`'s response parsing reuses Phase 3's already-confirmed shape, so it's lower-risk, but still needs a live execution this session can't perform.
- Verified with `tests/phase10-templates-verification.js` (Node-mocked `UrlFetchApp`): ADMIN-only draft management, edit/resubmit blocked once PENDING, sync upserts by `providerTemplateId` without duplicating, template send requires `APPROVED` and substitutes variables correctly. All thirteen test suites pass.
- Implemented Phase 11 Quick Replies & Media: `Phase11Api` (`src/Phase11Services.gs`) — quick replies (`createQuickReply`/`updateQuickReply` gated on `SETTINGS_MANAGE`, ADMIN-only; `listQuickReplies` open to any authenticated user), same two-tier pattern as Phase 8's stage definitions. `Phase6Api.sendMediaReply` (`src/Phase6Services.gs`) sends a media message via `ExotelProvider.sendMedia`, reusing the existing `sendOutbound_` helper, then writes a `Message_Media` record (new tab, same live-data-safety rationale as `Customer_Stage`/`Conversation_Snooze`). `ExotelProvider.processWebhook` (`src/Phase3ExotelProvider.gs`) now extracts a `mediaUrl` via a new `extractInboundMediaUrl_` helper; `Phase4Api.ingestInboundMessage` writes a `Message_Media` record for inbound messages that carry one.
- Added a quick-reply `<select>` (inserts text into the compose textarea rather than sending directly) and a "Media…" button (prompts for type/URL/caption) to `frontend/Index.html`'s compose row.
- `sendMediaReply` and `extractInboundMediaUrl_` are both unverified — no real media message has ever been sent or received through this integration (same real-world-cost/no-live-webhook-observed boundary as the rest of this session's Exotel-facing work).
- Verified with `tests/phase11-quick-replies-media-verification.js`: quick reply ADMIN-only create/update and open listing, `sendMediaReply` authorization (non-assigned agent denied) and request construction, outbound `Message_Media` record on both SENT and FAILED sends, inbound `Message_Media` record only when `mediaUrl` is present. All fourteen test suites pass.
- Implemented Phase 12 Admin Panel & Configuration: a second HTML page, `frontend/Admin.html`, served at `?page=admin` (`doGet`, `src/Phase5Endpoints.gs`), covering Dashboard (current-state counts only, not Phase 14's analytics), Users, Teams (plus a new `listTeamMembers(teamId)` endpoint the section needed), Numbers, Number Access, Assignment Rules, Lead Stages, Quick Replies, Templates, and Audit Log. Most sections are thin UI over each entity's already-existing service layer (Phase 1/3/8/10/11); the one genuinely new backend piece is `Phase12Api` (`src/Phase12Services.gs`) giving `Number_Assignment_Config`/`Number_Assignment_Users` their first admin-facing CRUD (`NUMBERS_ADMIN`-gated, upsert-style config), since only Phase 7's engine and tests had ever written them before.
- Closed the reassignment-UI gap explicitly deferred from Phase 7: `Phase7Api.listAssignableUsers(numberId)` (`src/Phase7Services.gs`) — ADMIN sees every active user, SUPERVISOR/SITE_MANAGER see active members of the team scoped to that number, anyone else denied — wired into a new "Reassign…" button in the inbox detail header (`frontend/Index.html`), not the Admin Panel, since it's a per-conversation inbox action.
- Added `whoAmI()` (`src/Phase1Services.gs`) so the client can decide what to show (the Admin Panel link, an access-denied message) — never the actual authorization boundary, every admin action still enforces its own permission server-side regardless.
- Verified with `tests/phase12-admin-panel-verification.js`: assignment-config upsert and validation, participant add/list/update and duplicate rejection, dashboard counts, and `listAssignableUsers`' three-way authorization split (ADMIN/team-scoped supervisor/denied agent). Added a `listTeamMembers` regression check to `tests/phase1-role-verification.js`. All fifteen test suites pass.
- Implemented Phase 13 Notifications, Search & Productivity: `Phase13Api` (`src/Phase13Services.gs`) — `searchConversations(filters)` (search by customer name/phone/message text, filter by assigned agent/status/needsResponse/unassigned/stage/date range, optionally across every accessible number at once) composes Phase 5's already-authorized `listMyNumbers`/`listConversations` rather than re-implementing access control, and `getNeedsResponseCounts()` for a per-number badge. "Notifications" scoped deliberately to that in-UI badge, not push/email — see `memory/DECISIONS.md`.
- Wired into `frontend/Index.html`: a filter bar (search box + Needs response/Unassigned checkboxes) above the Conversations pane, switching the list source between `listConversations` and `searchConversations` transparently, plus a red needs-response count badge per number that refreshes after every send/reassign/snooze action.
- Verified with `tests/phase13-search-notifications-verification.js`: search by name/phone/message text, every filter (assigned/status/needsResponse/unassigned/stage/date range), cross-number search, needs-response counts, and that search results still fully respect Phase 5's authorization (an agent with number access but no relationship to a specific conversation still can't find it via search). All sixteen test suites pass.
- Implemented Phase 14 Dashboard & Analytics: `Phase14Api` (`src/Phase14Services.gs`), gated on Phase 1's existing (previously unused) `REPORTS_VIEW` permission — conversation totals plus per-number and per-agent breakdowns, average first-response time (conversation `createdAt` → first `OUTBOUND` message), stage distribution, template usage (parsed from `Messages.messageText`'s `"[Template: name]"` marker, no schema change), and lead-conversion rate (customers currently in the `won` stage). `resolved` always reports `0` — no phase has ever added a way to close a conversation; rather than build that under an analytics phase, the gap is reported honestly and flagged in `PROGRESS.md`.
- Added a "Reports" link to `frontend/Index.html` (shown only if a `getDashboardMetrics()` probe succeeds, not a client-side role check) opening a full-page overlay with all the metrics above as tables.
- Verified with `tests/phase14-dashboard-verification.js`: AGENT denied (no `REPORTS_VIEW`), VIEWER/ADMIN see correct totals/breakdowns/response-time/stage-distribution/template-usage/conversion-rate numbers against a hand-built scenario. All seventeen test suites pass.
- Implemented Phase 15 Audit, Security, Backup & Reliability: confirmed the roadmap's minimum audited-event list is already fully covered by every prior phase's own audit calls (mapped in `docs/DATABASE.md` — no new logging code needed) and that secrets hygiene is clean (grepped source for hardcoded credentials — none found; `.gitignore` already excludes credential files). The one genuinely new piece: `Phase15Api` (`src/Phase15Services.gs`) — `backupNow()` (ADMIN, full timestamped spreadsheet copy into Drive) and `installDailyBackupTrigger()`/`removeDailyBackupTrigger()` (a time-driven Apps Script trigger, 2am Asia/Kolkata, calling the same backup logic via `performBackup_(null)` bypassing `AccessControl` entirely — a trigger has no Google identity, same reasoning as Phase 4's webhook). Added two new OAuth scopes to `appsscript.json` (`drive.file`, `script.scriptapp`) — will show a fresh consent screen next run.
- Added a "Backup" section to `frontend/Admin.html`: a "Back up now" button and daily-trigger enable/disable control.
- Verified with `tests/phase15-backup-verification.js` (mocked `SpreadsheetApp.copy`/`ScriptApp` triggers): `backupNow` ADMIN-only and creates a correctly-named copy, installing twice doesn't duplicate the trigger, removal works, and the trigger handler itself runs with no signed-in identity at all (simulating real trigger-execution context). All eighteen test suites pass. **Cannot live-verify the real Drive copy or trigger firing** — that requires an actual Apps Script execution, queued for the user.
- Implemented Phase 16 Testing & QA: added `tests/run-all.js` (consolidated runner, pass/fail summary across every suite) and `docs/TESTING.md` (coverage matrix against the roadmap's Phase 16 checklist, including two known/documented limitations — no true-concurrency testing since `LockService` is mocked as a no-op, and no conversation close/resolve workflow to test since it doesn't exist yet). Dispatched a background audit of every `PhaseNApi` method for missing authorization checks, which **found and confirmed one real security bug**: `Phase8Api.getCustomerStage(customerId)` had zero authorization check at all (any signed-in Google account, even with no `Users` record, could read any customer's lead stage). Fixed to match `setCustomerStage`'s own relationship check (ADMIN or a viewable conversation with that customer); added a regression test to `tests/phase8-crm-lite-verification.js`. Also added `tests/authorization-sweep-verification.js`, a static parser that checks all 68 public endpoints reference `this.access_` (directly, via a helper, or via a delegated `PhaseNApi`) — a permanent regression guard against the same class of mistake. All nineteen test suites pass (`node tests/run-all.js`).
- Phase 17/18 docs: `docs/DEPLOYMENT.md` (go-live readiness checklist), `docs/ZOHO_PHASE_2.md` (full entity mapping + 5 open questions), a Phase 15/16 addendum to `docs/SECURITY.md`. Phases 19-21 correctly left undone (blocked on Zoho credentials, real usage data, and the roadmap's own dependency ordering).
- **Live incident, resolved same day**: user reported the chatbot connected to number `079-485-02810` triggering for messages to *any* number, and after removing that webhook, customer messages no longer reflecting in the portal at all. Confirmed `phase4-webhook-ingestion`'s deployed version hadn't changed all session, ruling out our own code as the cause; gave the hypothesis that Exotel likely routes WhatsApp webhooks per-account/WABA rather than truly per-number, so the chatbot's URL (in that one shared slot) was intercepting every number's traffic, and removing it left nothing configured. User re-added our webhook URL to that slot and confirmed it's working again — no code change was needed.
- **Number-scoping reversal** (2026-08-10, same day as the redesign below, user-directed): the unified redesign had dropped the number-picker landing screen in favor of aggregating across every accessible number by default. The user reversed this — "not everything mixed" — so `#landingScreen` is back as the mandatory entry point, and `Phase14Api.getDashboardMetrics`, `Phase8Api.listCustomers`, `Phase9Api.listMyReminders` all gained an optional `numberId` param (defaulting to their prior aggregate behavior when omitted) so Dashboard/Inbox/All Conversations/Unassigned/Customers/Reminders/Reports/the notification bell/Previous Conversations all stay inside exactly one number once chosen. Org-wide admin pages (Users, Teams, Numbers, Templates, Quick Replies, Settings, Audit Log) stay unscoped on purpose. Added test coverage for all three new params. All 20 backend suites pass; pushed and redeployed (phase5-admin-ui @28).
- **Unified sidebar-nav redesign** (2026-08-10, user-directed, matching a full reference commercial WhatsApp CRM mockup the user shared): `frontend/Index.html` rebuilt from scratch as a single-page app — dark-green sidebar nav (Dashboard/Inbox/All Conversations/Unassigned/Reminders/Customers/Reports/Templates/Quick Replies/Teams/Users/WhatsApp Numbers/Settings/Audit Log), top bar (Availability dropdown wired to Phase 1's previously-unused `setAvailability`/`getAvailability`, notification bell), a KPI dashboard (real counts only, no fake trend deltas — user's explicit choice), and a redesigned chat panel (inline Assign dropdown replacing the old `prompt()` flow, Reply/Note tabs, sender names, inline media, Customer Details side panel with Previous Conversations/Notes/Reminders). Dropped the previous round's number-picker landing screen entirely — `searchConversations` already aggregates across every accessible number, so a forced "pick a number first" step was never necessary. Retired `frontend/Admin.html` and the `?page=admin` route entirely; every former admin page is now a sidebar section, ported directly from Admin.html's existing logic. Backend additions: `Phase8Api.listCustomers`/`updateCustomer`, `Phase13Api.searchConversations`'s `customerId` filter and `status: 'ANY'` bypass, `Phase14Api.getDashboardMetrics`'s `totalCustomers`/`assignedToMe`. **Caught and fixed a real bug during code review before shipping**: the first draft duplicated the list+chat+detail markup once per nav item (same element ids each time), which would have caused `getElementById` calls to silently target the wrong hidden page's copy — fixed by building exactly one shared instance and moving it between page slots via `appendChild`. All 20 backend test suites still pass (this was almost entirely a frontend change); pushed and redeployed (phase5-admin-ui @27).
- **Number/org-select landing screen** (2026-08-10, user-directed, matching a reference commercial WhatsApp CRM's UX the user shared screenshots of): `frontend/Index.html` restructured into two screens — `#landingScreen`, a card grid of accessible numbers (`listMyNumbers()`, with needs-response badges) as the new entry point, and `#workspaceScreen`, the existing Conversations+Detail two-pane view, now anchored to one number at a time with a small header showing the active number and a "← Switch number" link back to the card grid. The old persistent Numbers list pane is gone entirely — number selection now happens once, up front. Admin Panel/Reports links moved to the landing screen only.
- **Inbox polish** (2026-08-10, user-directed, prompted by real screenshots of the deployed workspace pane): the default conversation list now shows the customer's name (`Phase5Api.listConversations`/`listConversationsAllStatuses` enriched with `customerName`/`customerPhone`, matching what Phase 13's search already did); `WorkspaceApi` now resolves and returns `senderName` per message and `assignedUserName` for the conversation, so replies show who actually sent them and the header shows the real assignee instead of a raw ID; media messages now render an inline image or an icon+link (`WorkspaceApi` joins `Message_Media` onto each message) instead of literal `"[Media: image]"` text; Remarks/Reminders became collapsed-by-default `<details>` sections moved below the compose box, so the message thread and reply box are always visible without scrolling. Also a visual pass: status/assignment pills in the header, consistent `.action-btn` styling, WhatsApp-style bubble corners. No schema/authorization changes.
- **Post-Phase-18 user-directed follow-up** (2026-08-10, after the user reviewed the live system): (1) `Phase6Api.resolveConversation` — any assigned AGENT or ADMIN can mark a conversation resolved (same authorization tier as reply); `Phase5Api.listConversations` now excludes `CLOSED` conversations (new `listConversationsAllStatuses()` still finds them, used by Phase 13's search, defaulting to OPEN-only unless a status is explicitly requested); no manual reopen needed since a new inbound message already only ever attaches to an OPEN conversation. (2) `Phase14Api.getDashboardMetrics` is now scoped to the numbers a SUPERVISOR/SITE_MANAGER/VIEWER actually has access to (composes `Phase5Api.listMyNumbers()`) — **reverses** the earlier same-day decision to leave `REPORTS_VIEW` org-wide. (3) Diagnosed the user's "slow transitions" complaint as 8 separate `google.script.run` round-trips firing every time a conversation opened (each a full Apps Script execution with real cold-start latency) — fixed with a new `WorkspaceApi.getConversationWorkspace` (`src/WorkspaceServices.gs`) that aggregates everything into one call, plus client-side caching of templates/quick replies (stages were already cached). Declined a full Google-Sheets-to-database migration for now — the roadmap's own Phase 20 scopes that to after real usage data exists, and the diagnosed cause didn't require it. `frontend/Index.html` refactored accordingly (added a "Resolve" button). New test: `tests/workspace-verification.js`; extended `phase5`/`phase6`/`phase13`/`phase14` suites. All 20 test suites pass.
